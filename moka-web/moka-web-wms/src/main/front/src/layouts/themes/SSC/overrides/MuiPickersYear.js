import palette from '../palette';

export default {
    root: {
        fontSize: 16,
        '&:hover': {
            color: palette.basic.etc[2]
        }
    },
    yearSelected: {
        fontSize: 30,
        color: palette.basic.etc[2]
    }
};
