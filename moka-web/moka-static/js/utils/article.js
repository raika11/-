$(document).load( function() {


    // var image_width = $('.ab_photo').width();
    // if (540 > image_width){
    //     $('.ab_photo').attr('style', `width: ${image_width}`);

    // }
    
    // $(this).next('.caption').attr('style', `width: ${image_width}`);
});