import { Input, Select, Radio, CheckBox, Label } from '@components/Inputs';

const ComponentFactory = {
    Input,
    Select,
    Radio,
    CheckBox,
    Label,
};

export default ComponentFactory;
