import React from 'react';
import { MokaCard } from '@components';

const BulknList = () => {
    return (
        <>
            <MokaCard loading={null} header={false} width={725} className={'mr-gutter flex-fill'} bodyClassName="p-0 overflow-hidden">
                <>BulknList</>
            </MokaCard>
        </>
    );
};

export default BulknList;
