import commonStyle from '~/assets/jss/commonStyle';

const ReservedSearchStyle = (theme) => ({
    root: {
        height: 80,
        overflow: 'hidden',
        marginTop: '1px'
    },
    ...commonStyle
});

export default ReservedSearchStyle;
