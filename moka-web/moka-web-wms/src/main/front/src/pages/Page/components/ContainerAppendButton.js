import React from 'react';
import { MokaIcon } from '@components';

const ContainerAppendButton = (props) => {
    return <MokaIcon iconName="fal-file-plus" />;
};

export default ContainerAppendButton;
