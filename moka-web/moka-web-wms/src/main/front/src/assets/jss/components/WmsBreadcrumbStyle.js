/**
 * WmsBreadcrumb 컴포넌트 스타일
 * @param {object} theme 테마
 */
const WmsBreadcrumbStyle = (theme) => ({
    breadcrumbForm: {
        boxSizing: 'border-box',
        display: 'flex',
        alignItems: 'center',
        fontSize: '13px'
    },
    white: {
        color: '#fff'
    }
});

export default WmsBreadcrumbStyle;
